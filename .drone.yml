kind: pipeline
name: default

platform:
  os: linux
  arch: arm64
    
steps:
- name: test
  image: golang
  environment:
    DOCKER_USERNAME:
        from_secret: docker_username
    DOCKER_PASSWORD:
        from_secret: docker_password
  commands:
      - echo "hahah" 
      - echo -n $DOCKER_USERNAME
      - echo -n $DOCKER_PASSWORD